import { ControlSlideBtn } from "./ControlSlideBtn";

export const NextSlideBtn = (props) => {
  return (
      <span className="ion icon here">
        <i className="fa-regular fa-angle-left"></i>
      </span>
  );
};
